format_version: 10
pipelines:
  Frontend:
    group: defaultGroup
    materials:
      git-4210a58:
        git: https://github.com/Devops-Stage-1/expense-ansible.git
        shallow_clone: false
        auto_update: true
        branch: main
    stages:
    - dev:
        fetch_materials: true
        approval:
          type: success
          allow_only_on_success: false
        jobs:
          Test:
            timeout: 0
            tasks:
            - exec:
                arguments:
                - -i
                - frontend-dev.dkdevops.online
                - -e_username=ec2-user
                - -e_password=DevOps321
                - frontend.yml
                command: ansible-playbook
                run_if: passed
